{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && node --inspect ./node_modules/.bin/homebridge -I -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
